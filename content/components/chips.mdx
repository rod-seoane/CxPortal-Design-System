---
title: Chips & Tags
description: Compact inline labels for status, categorisation, and user input. Chips are interactive; Tags are primarily informational.
figmaNodeId: 188-8771
figmaUpdated: Sep 22, 2026
status: stable
---

## Overview

Chips and Tags are both compact inline elements, but they serve fundamentally different purposes. **Chips** are interactive — users create, select, filter, or dismiss them. **Tags** are primarily informational — they describe, classify, or indicate the state of content assigned by the system or a user.

## Key differences

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead> </TableHead>
      <TableHead>Chips</TableHead>
      <TableHead>Tags</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Purpose</strong></TableCell>
      <TableCell>Interactive elements for actions, selections, or filtering</TableCell>
      <TableCell>Metadata labels for categorisation and organisation</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Interactivity</strong></TableCell>
      <TableCell>Highly interactive — clickable, selectable, removable</TableCell>
      <TableCell>Primarily static or low interactivity — read-only, sometimes removable</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>User action</strong></TableCell>
      <TableCell>User creates or selects during a task</TableCell>
      <TableCell>System or user assigns to categorise content</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Context</strong></TableCell>
      <TableCell>Input controls, filters, selections</TableCell>
      <TableCell>Content descriptors, status indicators</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Behaviour</strong></TableCell>
      <TableCell>Can trigger actions, toggle states, or be dismissed</TableCell>
      <TableCell>Display information, may link to filtered views</TableCell>
    </TableRow>
  </TableBody>
</Table>

---

## Chip

### When to use

- When users need to make selections from a compact set of options
- For displaying and managing user inputs (such as tags in a form field)
- When filtering or refining content dynamically
- For representing complex entities in a compact form (e.g. a contact with avatar + name + remove action)
- When space is limited and traditional buttons would be too heavy

### Anatomy

- **Container** — 8px border-radius (Regular) or 4px (Small). Padding: 4px/12px (Regular) or 2px/8px (Small).
- **Left icon (optional)** — 12px (Regular) or 10px (Small) thin-weight icon.
- **Label** — 10px SemiBold, 0.4px letter-spacing (Regular). 8px SemiBold, 0.32px letter-spacing, capitalize-transformed (Small).
- **Dismiss icon (optional)** — 12px (Regular) or 10px (Small) × icon.

### Size

Chips come in two sizes. Regular is the default. Small (12px min-height, 8px text) exists for dense layouts — table cells, compact filter bars, inline metadata rows. Never mix sizes in the same group.

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Property</TableHead>
      <TableHead>Regular</TableHead>
      <TableHead>Small</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell>Min height</TableCell>
      <TableCell>—</TableCell>
      <TableCell>12px</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Padding</TableCell>
      <TableCell>4px 12px</TableCell>
      <TableCell>2px 8px</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Gap</TableCell>
      <TableCell>8px</TableCell>
      <TableCell>4px</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Border radius</TableCell>
      <TableCell>8px</TableCell>
      <TableCell>4px</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Font size</TableCell>
      <TableCell>10px</TableCell>
      <TableCell>8px</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Letter spacing</TableCell>
      <TableCell>0.4px</TableCell>
      <TableCell>0.32px</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Icon size</TableCell>
      <TableCell>12px</TableCell>
      <TableCell>10px</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Text transform</TableCell>
      <TableCell>—</TableCell>
      <TableCell>capitalize</TableCell>
    </TableRow>
  </TableBody>
</Table>

### Variants

#### Type & shade

Chips carry semantic meaning through colour — **5 types × 6 shades = 30 combinations**. Shade 100 is lightest; 600 is darkest. Grey was previously missing entirely, and shades 300/600 were missing from every type — both gaps are now filled in.

Shades 100/200/300 map directly onto each colour family's own 100/200/300 ramp step. Shades 400/500/600 *skip* — there's no literal "-400"/"-500" step defined for Info/Success/Warning/Error (those ramps only define 100/200/300/"default"(~500)/600/700), so shade 400 lands on the ramp's "-default"/"-500" step, shade 500 on "-600", and shade 600 on "-700". Grey/neutral is the one family with a real, literal 1:1 ramp, so it doesn't skip.

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Type</TableHead>
      <TableHead>Shade 100</TableHead>
      <TableHead>Shade 200</TableHead>
      <TableHead>Shade 300</TableHead>
      <TableHead>Shade 400</TableHead>
      <TableHead>Shade 500</TableHead>
      <TableHead>Shade 600</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Grey</strong></TableCell>
      <TableCell><code>#efefef</code></TableCell>
      <TableCell><code>#cdcdcd</code></TableCell>
      <TableCell><code>#adadad</code></TableCell>
      <TableCell><code>#8d8d8d</code></TableCell>
      <TableCell><code>#6f6f6f</code></TableCell>
      <TableCell><code>#525252</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Info</strong></TableCell>
      <TableCell><code>#d6e2f5</code></TableCell>
      <TableCell><code>#a4beea</code></TableCell>
      <TableCell><code>#5a89d8</code></TableCell>
      <TableCell><code>#1f4584</code></TableCell>
      <TableCell><code>#11274a</code></TableCell>
      <TableCell><code>#060d19</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Success</strong></TableCell>
      <TableCell><code>#ddf4d2</code></TableCell>
      <TableCell><code>#b5e89c</code></TableCell>
      <TableCell><code>#87d95e</code></TableCell>
      <TableCell><code>#4b9924</code></TableCell>
      <TableCell><code>#244a11</code></TableCell>
      <TableCell><code>#0c1906</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Warning</strong></TableCell>
      <TableCell><code>#fbeed8</code></TableCell>
      <TableCell><code>#f7ddb1</code></TableCell>
      <TableCell><code>#f1c780</code></TableCell>
      <TableCell><code>#eaa93c</code></TableCell>
      <TableCell><code>#c79033</code></TableCell>
      <TableCell><code>#2f220c</code></TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Error</strong></TableCell>
      <TableCell><code>#fbc6d4</code></TableCell>
      <TableCell><code>#f792ac</code></TableCell>
      <TableCell><code>#f3547d</code></TableCell>
      <TableCell><code>#ab0c36</code></TableCell>
      <TableCell><code>#690821</code></TableCell>
      <TableCell><code>#130106</code></TableCell>
    </TableRow>
  </TableBody>
</Table>

**Text colour is a hand-set matrix, not a simple "light shades get dark text" rule.** Every type uses dark text (`#1d1d1d`) through shade 300. Beyond that it varies by type — Info and Error switch to light text (`#f8f8f8`) starting at shade 400; Success does too, but only from shade 500; Warning stays on dark text all the way through shade 500 and only switches at 600; Grey switches at 500. Replicated exactly as read from the live component rather than a inferred pattern:

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Type</TableHead>
      <TableHead>100</TableHead>
      <TableHead>200</TableHead>
      <TableHead>300</TableHead>
      <TableHead>400</TableHead>
      <TableHead>500</TableHead>
      <TableHead>600</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow><TableCell><strong>Grey</strong></TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Light</TableCell><TableCell>Light</TableCell></TableRow>
    <TableRow><TableCell><strong>Info</strong></TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Light</TableCell><TableCell>Light</TableCell><TableCell>Light</TableCell></TableRow>
    <TableRow><TableCell><strong>Success</strong></TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Light</TableCell><TableCell>Light</TableCell></TableRow>
    <TableRow><TableCell><strong>Warning</strong></TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Light</TableCell></TableRow>
    <TableRow><TableCell><strong>Error</strong></TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Dark</TableCell><TableCell>Light</TableCell><TableCell>Light</TableCell><TableCell>Light</TableCell></TableRow>
  </TableBody>
</Table>

#### Chip types by function

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Type</TableHead>
      <TableHead>Description</TableHead>
      <TableHead>Example use case</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Input chip</strong></TableCell>
      <TableCell>Represents user-entered information that can be edited or removed</TableCell>
      <TableCell>Email recipients, search terms entered by user</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Choice chip</strong></TableCell>
      <TableCell>Single selection from a set of options (like radio buttons)</TableCell>
      <TableCell>Selecting a size: S, M, L, XL</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Filter chip</strong></TableCell>
      <TableCell>Multiple selections to filter content (like checkboxes)</TableCell>
      <TableCell>Filtering products by "In Stock", "On Sale", "New"</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Action chip</strong></TableCell>
      <TableCell>Triggers an action related to primary content</TableCell>
      <TableCell>"Add to calendar", "Share", "Save"</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Suggestion chip</strong></TableCell>
      <TableCell>Dynamically generated suggestions to assist user input</TableCell>
      <TableCell>Autocomplete suggestions, quick replies in chat</TableCell>
    </TableRow>
  </TableBody>
</Table>

---

## Tag

### When to use

- To display metadata about content items
- For showing status or state information alongside content
- When categorising items in lists or tables
- To indicate content type or classification
- For displaying system-assigned or pre-defined labels

### Anatomy

- **Container** — 16px border-radius (pill shape). Padding: 4px vertical, 12px horizontal.
- **Label** — 10px SemiBold, 0.4px letter-spacing.
- **Value (optional)** — Additional numeric or text value shown after the label.
- **Arrow (value-update only)** — 12px thin arrow separating old value from new value.

### Variants

#### States

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>State</TableHead>
      <TableHead>Background</TableHead>
      <TableHead>Text colour</TableHead>
      <TableHead>Use for</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Default</strong></TableCell>
      <TableCell><code>#cdcdcd</code> (neutral/200)</TableCell>
      <TableCell><code>#1d1d1d</code></TableCell>
      <TableCell>Standard, unvisited label</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Active</strong></TableCell>
      <TableCell><code>#6f6f6f</code> (neutral/500)</TableCell>
      <TableCell><code>#f8f8f8</code></TableCell>
      <TableCell>Currently selected or applied filter. Background was previously pointed at <code>--neutral-700</code> (<code>#373737</code>, far too dark) instead of <code>--neutral-500</code>.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Viewed</strong></TableCell>
      <TableCell><code>#8d8d8d</code> (neutral/400)</TableCell>
      <TableCell><code>#1d1d1d</code></TableCell>
      <TableCell>Previously visited or seen item</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Disabled</strong></TableCell>
      <TableCell><code>#f1f5ed</code> (surface/disabled)</TableCell>
      <TableCell><code>#adadad</code></TableCell>
      <TableCell>Interaction not allowed. Text was previously <code>--content-action-disabled-700</code> (<code>#6a7260</code>, a muddy dark green nowhere close to Figma's actual value) instead of the already-correct <code>--text-form-field-disabled</code>.</TableCell>
    </TableRow>
  </TableBody>
</Table>

Default and Viewed's dark text was previously `#373737` (routed through `--text-body-primary`, the same wrong-ramp-step alias found throughout this whole audit) instead of the correct `#1d1d1d` — bypassed to `--neutral-800` directly.

#### Types

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Type</TableHead>
      <TableHead>Layout</TableHead>
      <TableHead>Use for</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Simple</strong></TableCell>
      <TableCell>Label only</TableCell>
      <TableCell>Status labels, category names, plain metadata</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>With value</strong></TableCell>
      <TableCell>Label · Value</TableCell>
      <TableCell>Attribute–value pairs like "Audience 5" or "Version 2"</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Value update</strong></TableCell>
      <TableCell>Label · Old → New</TableCell>
      <TableCell>Showing a pending or applied change — "Count 2 → 5"</TableCell>
    </TableRow>
  </TableBody>
</Table>

#### Tag types by function

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Type</TableHead>
      <TableHead>Description</TableHead>
      <TableHead>Example use case</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><strong>Categorical</strong></TableCell>
      <TableCell>Classifies content into predefined categories</TableCell>
      <TableCell>"master list", "documentation", "archived"</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Status</strong></TableCell>
      <TableCell>Indicates the current state of an item</TableCell>
      <TableCell>"published", "draft", "synched", "pending"</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Attribute</strong></TableCell>
      <TableCell>Displays a specific attribute or property of content</TableCell>
      <TableCell>"audience: external", "customer: enterprise"</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><strong>Count / Badge</strong></TableCell>
      <TableCell>Shows numerical information related to the item</TableCell>
      <TableCell>Notification count, item quantity</TableCell>
    </TableRow>
  </TableBody>
</Table>

---

## Decision framework

**Use a Chip when:**
- The user needs to interact with the element
- The element represents user input that can be modified or removed
- You need selection behaviour (single or multiple)
- The element triggers an action
- You are building a filter interface

**Use a Tag when:**
- The element is primarily informational
- You are displaying metadata or categorisation
- You need to show status or state
- The label is system-defined rather than user-created
- The element describes content rather than controls it

---

## Do's & Don'ts

<DosDonts>
  <Do description="Use Info shade 100 or 200 for chips embedded in light backgrounds — they provide colour-coded meaning without overwhelming the surrounding content." />
  <Dont description="Use shade 400 or 500 chips in large quantities on a single page. High-contrast shades draw strong attention; reserve them for critical or high-priority states." />
</DosDonts>

<DosDonts>
  <Do description="Always attach an onDismiss handler when the × icon is shown. A dismiss icon with no action confuses users who expect to remove the chip." />
  <Dont description="Show the dismiss icon on chips that are not removable (such as read-only filter labels). Remove iconRight if the chip has no removal behaviour." />
</DosDonts>

<DosDonts>
  <Do description="Use Tags in table cells and list metadata to give users at-a-glance context about each row's state, category, or attribute." />
  <Dont description="Use Tags as primary call-to-action elements. Their low visual weight and read-only intent make them poor substitutes for buttons or chips when an action is required." />
</DosDonts>

<DosDonts>
  <Do description="Use the value-update Tag type to communicate a pending or recently applied change — for example, showing that a count went from 2 to 5 after a sync." />
  <Dont description="Use value-update Tags for static data. This type implies change is in progress or has just occurred; using it for stable values misleads users about the data's state." />
</DosDonts>

<DosDonts>
  <Do description="Keep chip and tag labels short — one to three words. They are scanned, not read, and long labels break the horizontal rhythm of the layout." />
  <Dont description="Truncate chip or tag labels with an ellipsis. If the text doesn't fit, shorten the label or use a tooltip — hidden text defeats the informational purpose." />
</DosDonts>

---

## Code

### Chip — basic

```tsx
import { Chip } from '@/components/ui/chip'

<Chip label="Current" type="info" shade={100} />
<Chip label="Active"  type="success" shade={200} />
<Chip label="Pending" type="warning" shade={400} />
<Chip label="Blocked" type="error" shade={500} />
```

### Chip — dismissible (controlled)

```tsx
const [tags, setTags] = useState(['Design', 'Research', 'Dev'])

{tags.map((tag) => (
  <Chip
    key={tag}
    label={tag}
    type="info"
    shade={100}
    onDismiss={() => setTags((t) => t.filter((x) => x !== tag))}
  />
))}
```

### Chip — icon variants

```tsx
{/* No icons — label only */}
<Chip label="Draft" type="warning" shade={200} iconLeft={false} iconRight={false} />

{/* Left icon only */}
<Chip label="Connected" type="success" shade={100} iconRight={false} />
```

### Chip — clickable

```tsx
<Chip
  label="Filter by status"
  type="info"
  shade={100}
  onClick={() => openFilterPanel()}
/>
```

### Chip/Small — dense layout

```tsx
<Chip label="Current" type="info" shade={100} size="small" />
<Chip label="Synched" type="success" shade={200} size="small" />
<Chip label="Warning" type="warning" shade={300} size="small" iconLeft={false} />
```

### Tag — simple

```tsx
import { Tag } from '@/components/ui/chip'

<Tag label="Published"  state="default" />
<Tag label="Active"     state="active" />
<Tag label="Viewed"     state="viewed" />
<Tag label="Locked"     state="disabled" />
```

### Tag — with value

```tsx
<Tag label="Audience" type="with-value" value="5" state="default" />
<Tag label="Version"  type="with-value" value="v1.2" state="active" />
```

### Tag — value update

```tsx
{/* Shows a change from one value to another */}
<Tag
  label="Count"
  type="value-update"
  value="2"
  newValue="5"
  state="default"
/>
```

---

## Token reference

<Table size="compact" className="mb-6">
  <TableHeader>
    <tr>
      <TableHead>Token</TableHead>
      <TableHead>Value</TableHead>
      <TableHead>Usage</TableHead>
    </tr>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><code>--info-100</code> … <code>--info-700</code></TableCell>
      <TableCell>see Type & shade table above</TableCell>
      <TableCell>Chip Info backgrounds. Chip shade 400/500/600 map onto <code>--info-500</code>/<code>-600</code>/<code>-700</code> — not <code>--info-default</code>, which the previous implementation used for shade 400 and doesn't match Figma's value there.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--success-100</code> … <code>--success-700</code></TableCell>
      <TableCell>see Type & shade table above</TableCell>
      <TableCell>Chip Success backgrounds. Same shade-skip pattern as Info.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--warning-100</code>, <code>--warning-default</code>, <code>--warning-500</code>, <code>--warning-700</code></TableCell>
      <TableCell>see Type & shade table above</TableCell>
      <TableCell>Chip Warning backgrounds. Warning has no <code>-600</code> token at all in this codebase — shade 600 uses <code>--warning-700</code> directly, matching Figma exactly.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--error-100</code> … <code>--error-700</code></TableCell>
      <TableCell>see Type & shade table above</TableCell>
      <TableCell>Chip Error backgrounds. Same shade-skip pattern as Info.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--neutral-100</code> … <code>--neutral-600</code></TableCell>
      <TableCell>see Type & shade table above</TableCell>
      <TableCell>Chip Grey backgrounds — previously missing entirely, no Grey type existed in code at all.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--neutral-200</code></TableCell>
      <TableCell><code>#cdcdcd</code></TableCell>
      <TableCell>Tag Default state background</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--neutral-400</code></TableCell>
      <TableCell><code>#8d8d8d</code></TableCell>
      <TableCell>Tag Viewed state background</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--neutral-500</code></TableCell>
      <TableCell><code>#6f6f6f</code></TableCell>
      <TableCell>Tag Active state background. Previously <code>--neutral-700</code> (<code>#373737</code>).</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--surface-disabled</code></TableCell>
      <TableCell><code>#f1f5ed</code></TableCell>
      <TableCell>Tag Disabled state background. Previously <code>--neutral-100</code> (<code>#efefef</code>) — a different, wrong token.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--neutral-800</code></TableCell>
      <TableCell><code>#1d1d1d</code></TableCell>
      <TableCell>Dark label text (Chip and Tag). Figma names this <code>text/on-action/secondary</code>, but that shared alias resolves to the wrong ramp step (<code>#373737</code>) in this codebase — bypassed directly, the same recurring bug found throughout this whole audit.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--text-form-field-disabled</code></TableCell>
      <TableCell><code>#adadad</code></TableCell>
      <TableCell>Tag Disabled state text. Previously <code>--content-action-disabled-700</code> (<code>#6a7260</code>, unrelated and much darker).</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code>--text-on-action-primary</code></TableCell>
      <TableCell><code>#f8f8f8</code></TableCell>
      <TableCell>Light label text on dark backgrounds (Chip and Tag). Already resolves correctly in this codebase — no bypass needed.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Font size</TableCell>
      <TableCell>10px</TableCell>
      <TableCell>Label text size for both Chip and Tag</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Font weight</TableCell>
      <TableCell>600 (SemiBold)</TableCell>
      <TableCell>Label text weight for both Chip and Tag</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Letter spacing</TableCell>
      <TableCell>0.4px</TableCell>
      <TableCell>Label tracking for both Chip and Tag</TableCell>
    </TableRow>
  </TableBody>
</Table>

---

## Accessibility

- Chips that are clickable use `role="button"` and `tabIndex={0}` so they receive keyboard focus. `Enter` and `Space` trigger the click handler.
- The dismiss (×) icon uses `role="button"` with `aria-label="Remove {label}"` so screen readers announce its purpose.
- Chips and Tags do not rely on colour alone to convey meaning — always pair a semantic type with a visible text label.
- Disabled Tags carry `aria-disabled="true"` to communicate non-interactivity to assistive technologies.
- When Chips are used as filter toggles in a group, wrap them in a `role="group"` with an `aria-label` that names the filter set (e.g. `aria-label="Filter by status"`).
- For removable input chips (e.g. email recipients), announce removals with an `aria-live="polite"` region so screen readers confirm the item was removed.

## Open questions

- **Figma's own Anatomy prose claims more states than the actual component has.** The Principles doc (`2995-1025`) says Chip "supports default, selected, hover, focused, and disabled states," but the real Chip component (`188-8771`) exposes only `type` (colour family) and `colorValue` (shade) as its full prop surface — no state variants of any kind exist on the live component. Not built — there's nothing concrete to implement against, and inventing a selected/hover/focus treatment with no Figma backing would be guessing, not implementing.
- **Chip's colour `type` and Principles' functional taxonomy are two different axes, both called "type."** Principles describes "Five Chip types" as a functional/behavioural taxonomy — Input, Choice, Filter, Action, Suggestion — orthogonal to the five *colour* families (Grey/Info/Success/Warning/Error) this component's `type` prop actually controls. The functional taxonomy isn't a prop on the real Figma component either (it's a Principles-level categorisation of *how a chip is used*, not a visual variant), so it isn't modelled in code — a consumer picks the right colour family and wires up the right behaviour (dismissible, clickable, etc.) to match whichever functional role they need.
- **Usage requires `aria-pressed`/`aria-selected` for Choice/Filter chips**, but since there's no selection-state concept on the real component (see above), this component doesn't expose one either. A consumer building a Choice or Filter chip group should add `aria-pressed` on their own clickable wrapper — this component's `onClick` alone doesn't communicate selection state.
